machine:
  php:
    version: 8.4.5

general:
  artifacts:
    - "build/logs/clover.xml"

test:
  override:
    - vendor/bin/phpunit --coverage-clover build/logs/clover.xml
    - composer quality
  post:
    - vendor/bin/coveralls -v
